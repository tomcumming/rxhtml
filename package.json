{
  "scripts": {
    "format": "prettier --write package.json tsconfig.json src/**/*",
    "check": "tsc",
    "dev": "esbuild src/main.ts --servedir=www --outdir=www/js --bundle --format=esm"
  },
  "dependencies": {
    "esbuild": "^0.8.51",
    "prettier": "^2.2.1",
    "rxjs": "^6.6.3"
  }
}
